# TODO

Pending engineering tasks captured during day-to-day work. Move items out
to plans / weeklies / commit messages as they're picked up.

## Dependency management

- **Pin runtime dependencies.** `hop3-server` (and the other workspace
  packages) currently use loose floors like `litestar[standard]>=2.22.0`.
  Hop3 is an app, not a library — runtime deps should be pinned to exact
  versions, with controlled bumps. This avoids two failure modes seen in
  production: (1) pip's `only-if-needed` upgrade strategy leaving stale
  transitive deps on the server (e.g. litestar 2.18 satisfying `>=2.18.0`
  while local code uses 2.22-only APIs), and (2) silent drift between
  developer machines and the production server.

  Likely shape: `requirements.lock`-style file generated by `uv pip
  compile` (or `uv lock` with `package = true` flipped), used by the
  installer's pip step and pinned at PR time.

  Scope: probably ship as part of the 0.5.0 release prep, after the
  migration-gate work lands.

## (other items as they come up)
