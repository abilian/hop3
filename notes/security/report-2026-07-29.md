# Security review, 2026-07-29

Audited 255 files (1.9 MB) against `notes/security/threat-model.md`. The byte ceiling truncated the listing; `hop3-installer` and `hop3-testing` are excluded by config and not in scope for the service threat model. 8 candidate findings, **3 confirmed**, 4 refuted by verification, 1 needing input.

**Remediated 2026-08-01.** All three confirmed findings are fixed; F6 remains open pending a decision. The fixes are recorded under each finding, and the invariants they established are in [security-model.md](security-model.md) §3.8 and §3.9. Of the four threat-model gaps this round named, three are closed and one (SMTP credentials vs. the encryption invariant) is open; see [backlog-2026-08-01.md](backlog-2026-08-01.md) §2.2.

## Confirmed findings

### F1: Critical: No rate limiting on RPC `auth get-token`; brute-force at wire speed

**File:** `packages/hop3-server/src/hop3/commands/auth.py:61`
**CWE:** CWE-307 (Improper Restriction of Excessive Authentication Attempts)
**Status:** **Fixed** (2026-08-01). Was NEW; not reported in May or July 2026 rounds.

`AuthGetTokenCmd.requires_auth = False` allows unauthenticated calling via `POST /rpc`. The command performs bcrypt password verification at `auth.py:89` with **no per-IP rate limiting**. The web login path (`POST /auth/login`) applies `_AUTH_RATE_LIMITER.check()` at `server/controllers/auth.py:120` (5 attempts/minute/IP); the RPC path bypasses it entirely.

**What this means:** An unauthenticated attacker can attempt credentials at the speed of bcrypt hashing (~5-10/second on a modern CPU), roughly a 100x amplification over the 5/minute throttle. Combined with F2 (user enumeration), an attacker can first enumerate valid usernames, then brute-force their passwords.

**Fixed.** The limiter instance and `client_ip()` moved from `server/controllers/auth.py` into `server/security/rate_limit.py` as `AUTH_RATE_LIMITER`, and the RPC dispatcher checks it before dispatch. Both transports now draw down one budget.

Of the three options the finding offered, the second (a separate limiter for the RPC surface) would not have fixed it: two `RateLimiter(5, 60)` instances give an attacker 5 attempts per transport per minute, so the ceiling doubles and the control is still per-transport. The instance being shared is the fix. The third option (check inside `AuthGetTokenCmd.call`) fails differently: the command has no access to the request, so it cannot key on client IP.

The dispatcher decides *which* commands to throttle from a declarative `Command.rate_limited` flag. A name check would miss an alias and the next pre-auth command someone writes. A test (`tests/a_unit/commands/test_rate_limited_commands.py`) fails when a `requires_auth = False` command appears that neither sets the flag nor is listed as verifying no credential; it caught one command (`help commands`) that this fix had otherwise overlooked. That test is the actual remediation; the limiter call is just the first thing it enforces.

The throttle honours `HOP3_UNSAFE`, mirroring the auth skip in `_check_authentication`; the flag cannot be set in production (`core/unsafe_gate.py` forces it off, §3.3.1) and test suites log in far faster than 5/min.

---

### F2: High: User enumeration via distinct error messages in RPC `auth get-token`

**File:** `packages/hop3-server/src/hop3/commands/auth.py:81-90`
**CWE:** CWE-204 (Observable Response Discrepancy)
**Status:** **Fixed** (2026-08-01). Was NEW; not reported in May or July 2026 rounds.

`AuthGetTokenCmd.call()` returns three distinguishable outcomes:
1. Line 81-82: User not found → `"Invalid username or password"`
2. Line 85-86: User exists, inactive → `"Account is disabled"`
3. Line 89-90: User exists, wrong password → `"Invalid username or password"`

An attacker can detect disabled accounts (distinct message). Additionally, timing: `get_by_username` is ~milliseconds; `check_password` is bcrypt (~hundreds of ms). An attacker can differentiate "user exists, wrong password" from "user doesn't exist" by response time.

The web login path (`server/controllers/auth.py:143`) correctly collapses all failure modes into a single `"Invalid username or password"` message. The RPC path diverges.

**Fixed**, both halves. The three branches return one identical response object, and `orm/security.py::burn_password_check` verifies against a throwaway hash on the two branches with no real hash to check, so the timing matches too. The CPU concern the finding raises is answered by F1's limiter; the two fixes only work together, which §3.9 of the security model now says explicitly.

An existing test asserted the vulnerable behaviour (`test_auth_login_inactive_user` required the word "disabled" in the reply), so the fix had to invert it. A test can pin a defect in place; a green suite is not evidence that a security property holds unless a test asserts that property. The replacement asserts all three failures are byte-identical.

---

### F4: Medium: Admin-scoped JWT issued via un-rate-limited RPC path

**File:** `packages/hop3-server/src/hop3/commands/auth.py:99-103`
**CWE:** CWE-307 (Improper Restriction of Excessive Authentication Attempts)
**Status:** **Fixed** (2026-08-01) by F1. Was NEW; related to F1, scored separately for the admin-scope blast radius.

The same un-rate-limited RPC path at `auth.py:99-103` appends the `admin` scope to the JWT when the authenticated user is an admin. The web login path constrains admin brute-force via rate limiting; the RPC path applies no such constraint, and no additional factor (such as forced 2FA, stricter rate limiting, or admin-notification on token issuance) is applied.

**Fixed** by F1; the path is throttled, so the admin scope is no longer reachable by unmetered guessing.

The two extras the finding suggests were **not** done, deliberately. A stricter limit keyed on `is_admin` cannot be applied before the password is verified without revealing which accounts are admins; it would reintroduce F2 in a new place. Notification on admin token issuance belongs with the control-plane audit log ([report-2026-07-21.md](report-2026-07-21.md), planned for 0.8), not bolted onto one command.

---

## Gone since last time

All four July 2026 findings confirmed gone:

| July finding | July status | This audit |
|---|---|---|
| #2: Installer input validation | Fixed `10998740` | Confirmed intact (regex guards at config.py __post_init__) |
| #3: `run_as_hop3` shell sink | Fixed | Confirmed intact (list-form API + string rejection) |
| #4: psql -f containment | Fixed | Confirmed intact (resolve_backup_file at all three engines) |
| #5: Auth rate limiter per-process | Fixed | Confirmed intact (WORKERS=1 pinned + startup assertion) |
| #6: StrictHostKeyChecking | Documented | Default unchanged (by decision) |

May 2026 findings (#1, #3, #4) concerning magic-link scope, addon SQL injection, proxy hostname validation; all confirmed still mitigated.

## Needs input

### F6: Low: Magic link token consumed before user-existence validation

**File:** `packages/hop3-server/src/hop3/server/security/tokens.py:391`

`validate_magic_token` revokes the token at line 391 *before* checking that the user exists and is active at `auth.py:267-276`. If the user was deleted between generation and redemption, the token is burned without granting access. Low practical impact (5-minute TTL, no data loss, attacker burns their own token). The security-model.md §3.7 notes the fix path (make logout a POST), and this follows the same pattern of an operation that should be order-reversed. Needs a design decision: move the `get_by_username` + `active` check before `revoke_token`, or document as intentional (token already consumed = cannot be replayed, even if redemption fails).

## Refuted

4 findings refuted by clean-context verification. The refutations:

| ID | Finding | Refutation reason |
|----|---------|-------------------|
| F3 | No CSRF tokens on dashboard POSTs | The threat model's "Out of scope" table explicitly names this: CSRF tokens excluded because every mutation is a POST + `samesite=lax`. This is an accepted risk, not an oversight. |
| F5 | Logout via GET enables CSRF forced-logout | Also in the threat model's "Out of scope": "`GET /auth/logout` is the known gap." Documented since May 2026. |
| F7 | sync_to_thread=False on blocking handlers | Blocks the event loop but is not a security defect; the authenticated user is operator-equivalent and only degrades their own dashboard. App serving is independent (uWSGI emperor, not ASGI). |
| F8 | Dead RPC git-hook path uses raw tar | The path reads `sys.stdin` (the server process's fd, `/dev/null` under systemd), which is not attacker-controllable through JSON-RPC. Returns before reaching `tar -xf`. Live path (SSH post-receive → `server/cli/git.py`) uses `extract_archive_to_dir`. |

All refutations accepted.

## Gaps in the threat model

1. ~~**`auth get-token` via `POST /rpc` is not listed as a pre-auth entry point.**~~ **Closed**; the threat model's entry-point table now names it as its own row. Listing only `POST /rpc` in the table made the endpoint read as gated-by-auth when one command on it is pre-auth. The command, distinct from the transport, is the row that matters.

2. ~~**`GET /auth/login` (login page render) not listed.**~~ **Already closed** at the time of writing: the entry-point table has the row.

3. **SMTP plaintext password gap not carved into the credential-encryption invariant.** Security-model.md §3.4 documents the gap ("Encryption at rest is deferred platform-wide (M3.8)"), but this exception does not appear in the threat model's credential-encryption invariant or Out of scope sections. The model should note it.

4. ~~**SSH git push → post-receive hook path.**~~ **Already closed** at the time of writing: the trust-boundary table carries the "SSH git push → app deploy" row with the forced command, the path-derived app name and `git archive` extraction.

Gaps 1 and 2 are closed; 3 stands open. Invariant 7 says credentials are encrypted at rest, while `plugins/email/email.py` holds `smtp_password` as a plain `str` and injects it into app environments as `SMTP_PASSWORD` / `EMAIL_HOST_PASSWORD` / `MAIL_PASSWORD`. The exception needs carving into the invariant, or the invariant is wrong as written.

## Coverage

**Audited:** `packages/hop3-rootd/src/`, `packages/hop3-server/src/`, `packages/hop3-cli/src/` (255 source files).

**What the byte ceiling dropped:** Files after ~1.99 MB in the `audit_scope` listing. Based on the file list, this is late in the server plugin tree; the proxy plugins and addon plugins were captured. The email plugin and Docker build/deploy plugins were partially covered. `hop3-installer` and `hop3-testing` are not in the configured scope.

**Not covered by this review:**
- The `hop3-installer` package (correctly excluded from `audit_scope` source list for the service model; the `hop3-install` / `hop3-deploy-server` tools were not re-audited)
- The `hop3-testing` package (development tooling, not production)
- The `hop3-tui` package (not in source list; shares threat posture with CLI)
- Dynamic plugin loading at runtime (only statically analyzable plugin code was traced)
- OS-level sandboxing (systemd unit configuration, cgroup setup; outside the Python source)
- Nix builder plugin paths (not in the scanned files; the Nix builder is in `plugins/nix/` which may have been beyond the byte ceiling)
