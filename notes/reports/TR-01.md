# Hop3 Technical Report TR-01

**Interim Technical Report for NGI Project M5.3**

**Version:** 1.0 (Draft)
**Date:** February 2026
**Authors:** Abilian SAS
**Project:** Nix Integration for Hop3 (NGI0 Commons Fund)

---

## Executive Summary

Hop3 is an open-source Platform as a Service (PaaS) that enables deployment and management of web applications on single servers. Designed for digital sovereignty and simplicity, Hop3 provides a production-ready alternative to complex container orchestration systems, targeting small-to-medium businesses, managed service providers, and developers who value control over their infrastructure.

This interim technical report documents the architecture, implementation status, and security features of Hop3 as of February 2026, covering work completed under the NGI0 Commons Fund grant.

**Key Achievements:**
- Client-server architecture with JSON-RPC communication
- Plugin-based extensibility for builders, deployers, and addons
- Support for 9 programming languages across 33 deployment tutorials
- Comprehensive security features including JWT authentication and encrypted credential storage
- 638 automated tests with 100% pass rate on unit/integration layers

---

## Table of Contents

1. [Introduction](#1-introduction)
2. [Architecture Overview](#2-architecture-overview)
3. [Package Structure](#3-package-structure)
4. [Plugin System](#4-plugin-system)
5. [Security Features](#5-security-features)
6. [Testing Infrastructure](#6-testing-infrastructure)
7. [Deployment Support](#7-deployment-support)
8. [Documentation](#8-documentation)
9. [Project Status](#9-project-status)
10. [Future Work](#10-future-work)
11. [Conclusion](#11-conclusion)

---

## 1. Introduction

### 1.1 Project Background

Hop3 originated from the observation that modern web application deployment has become unnecessarily complex. While container orchestration platforms like Kubernetes offer powerful capabilities, they introduce significant operational overhead that many organizations cannot justify. Hop3 addresses this gap by providing:

- **Simplicity**: Deploy applications with familiar tools (git push, CLI commands)
- **Sovereignty**: Self-host on your own infrastructure without vendor lock-in
- **Security**: Built-in authentication, encrypted credentials, and security best practices
- **Sustainability**: Runs efficiently on modest hardware without container overhead

### 1.2 Target Users

Hop3 serves multiple user communities:

| User Type | Primary Needs |
|-----------|---------------|
| **SMB IT Administrators** | Centralized management, easy migrations, reliable backups |
| **Managed Service Providers** | Multi-tenant deployments, scalable client management |
| **Developers/DevOps** | CI/CD integration, consistent environments, rapid iteration |
| **Educational Institutions** | Manageable infrastructure, secure data handling |
| **Non-profits** | Cost-effective solutions, minimal technical expertise required |

### 1.3 Design Principles

Hop3 development follows these core principles:

1. **Convention over Configuration**: Sensible defaults reduce boilerplate
2. **12-Factor App Compliance**: Environment-based configuration, stateless processes
3. **Plugin-First Architecture**: Core functionality extended through plugins
4. **Fail-Fast Behavior**: Early detection and clear error messages
5. **Security by Default**: Authentication required, credentials encrypted

---

## 2. Architecture Overview

### 2.1 High-Level Architecture

Hop3 employs a client-server architecture where the CLI client communicates with the server via JSON-RPC over SSH tunnels or HTTP/HTTPS.

```
┌─────────────────┐     SSH/HTTP      ┌─────────────────────────────────┐
│   Developer     │◄─────────────────►│         Hop3 Server             │
│   Workstation   │    JSON-RPC       │                                 │
│                 │                   │  ┌─────────┐  ┌─────────────┐   │
│  ┌───────────┐  │                   │  │ Litestar│  │  Pluggy     │   │
│  │ hop3-cli  │  │                   │  │  ASGI   │  │  Plugins    │   │
│  └───────────┘  │                   │  └────┬────┘  └──────┬──────┘   │
│                 │                   │       │              │          │
│  ┌───────────┐  │                   │  ┌────▼──────────────▼──────┐   │
│  │ hop3-tui  │  │                   │  │     SQLAlchemy ORM       │   │
│  └───────────┘  │                   │  └────────────┬─────────────┘   │
└─────────────────┘                   │               │                 │
                                      │  ┌────────────▼─────────────┐   │
                                      │  │   SQLite / PostgreSQL    │   │
                                      │  └──────────────────────────┘   │
                                      │                                 │
                                      │  ┌──────────────────────────┐   │
                                      │  │   Nginx / Caddy / Traefik│   │
                                      │  └──────────────────────────┘   │
                                      │                                 │
                                      │  ┌──────────────────────────┐   │
                                      │  │   uWSGI Process Manager  │   │
                                      │  └──────────────────────────┘   │
                                      └─────────────────────────────────┘
```

### 2.2 Communication Flow

1. **CLI/TUI** parses user commands and validates input
2. **SSH Tunnel** established for secure communication (or direct HTTPS)
3. **JSON-RPC** request sent to server endpoint
4. **Server** authenticates request via JWT token
5. **Command Handler** processes request through appropriate plugins
6. **Response** returned to client with structured output

### 2.3 Deployment Flow

```
git push / hop3 deploy
        │
        ▼
┌───────────────────┐
│  Archive Upload   │
└────────┬──────────┘
         │
         ▼
┌───────────────────┐
│  Build Phase      │
│  (Toolchains)     │
└────────┬──────────┘
         │
         ▼
┌───────────────────┐
│  Deploy Phase     │
│  (uWSGI/Static)   │
└────────┬──────────┘
         │
         ▼
┌───────────────────┐
│  Proxy Config     │
│  (Nginx/Caddy)    │
└────────┬──────────┘
         │
         ▼
┌───────────────────┐
│  Health Check     │
└───────────────────┘
```

---

## 3. Package Structure

Hop3 is organized as a monorepo with five main packages:

### 3.1 hop3-server

The core server component that runs on deployment targets.

| Component | Technology | Purpose |
|-----------|------------|---------|
| Web Framework | Litestar (ASGI) | HTTP API and web UI |
| Database | SQLAlchemy + Advanced Alchemy | ORM and migrations |
| Plugin System | Pluggy | Extensibility framework |
| Dependency Injection | Dishka | Service container |
| Process Manager | uWSGI | Application runtime |

**Key Responsibilities:**
- JSON-RPC API for CLI/TUI communication
- Application deployment orchestration
- Reverse proxy configuration
- Database addon management
- User authentication and authorization

### 3.2 hop3-cli

Thin client for remote server management.

**Features:**
- JSON-RPC over SSH tunneling
- Rich terminal output with color formatting
- Confirmation prompts for destructive operations
- Local configuration management

### 3.3 hop3-installer

Installation and deployment toolkit.

| Command | Purpose | User |
|---------|---------|------|
| `hop3-install cli` | Install CLI on local machine | End users |
| `hop3-install server` | Install server on target machine | Sysadmins |
| `hop3-deploy` | Deploy/update during development | Developers |

**Design Constraint:** No runtime dependencies (stdlib only) to ensure reliable bootstrap.

### 3.4 hop3-tui

Terminal user interface built with Textual.

**Features:**
- Real-time dashboard with system metrics
- Application management interface
- Live log streaming
- Environment variable editing

### 3.5 hop3-testing

Test framework and fixtures for E2E testing.

**Capabilities:**
- Unified test runner (`hop3-test` CLI)
- Docker-based test infrastructure
- Test catalog with `test.toml` support
- Multiple target types (Docker, SSH, ready images)

---

## 4. Plugin System

Hop3 uses [Pluggy](https://pluggy.readthedocs.io/) for its plugin architecture, enabling extensibility without core modifications.

### 4.1 Plugin Types

| Type | Purpose | Examples |
|------|---------|----------|
| **Builders** | Orchestrate build process | LocalBuilder, DockerBuilder |
| **Language Toolchains** | Language-specific build logic | Python, Node, Ruby, Go, Rust, Java, PHP |
| **Deployers** | Run built artifacts | uWSGI, Docker Compose, Static |
| **Addons** | Backing services | PostgreSQL, MySQL, Redis |
| **Proxies** | Reverse proxy configuration | Nginx, Caddy, Traefik |
| **OS Implementations** | Operating system abstraction | Debian family, Red Hat family |
| **Health Checks** | Service health monitoring | Database connectivity checks |

### 4.2 Hook Specifications

Plugins implement hooks defined in `hop3/core/hookspecs.py`:

```python
@hookspec
def get_builders() -> list[type[Builder]]:
    """Return builder implementations."""


@hookspec
def get_language_toolchains() -> list[type[LanguageToolchain]]:
    """Return language toolchain implementations."""


@hookspec
def get_addons() -> list[type[Addon]]:
    """Return addon implementations."""


@hookspec
def get_health_checks() -> list[HealthCheck]:
    """Return health check implementations."""
```

### 4.3 Example: Health Check Plugin

```python
class PostgresHealthCheck:
    name = "postgresql"

    def is_configured(self) -> bool:
        return PostgresAdmin.from_config().password is not None

    def check(self) -> HealthCheckResult:
        try:
            # Verify PostgreSQL connectivity
            admin = PostgresAdmin.from_config()
            admin.test_connection()
            return HealthCheckResult(
                name="PostgreSQL", passed=True, message="Connection successful"
            )
        except Exception as e:
            return HealthCheckResult(name="PostgreSQL", passed=False, message=str(e))


class PostgresPlugin:
    @hookimpl
    def get_health_checks(self) -> list:
        return [PostgresHealthCheck()]
```

---

## 5. Security Features

### 5.1 Authentication

**JWT Token Authentication:**
- Tokens issued upon successful login
- Configurable expiration (default: 24 hours)
- Token revocation support for logout
- Bcrypt password hashing (cost factor 12)

**Role-Based Access Control:**
- Admin and user roles
- Permission checks on sensitive operations
- Audit logging for security events

### 5.2 Credential Management

**Fernet AEAD Encryption:**
- Service credentials encrypted at rest
- Key derivation via PBKDF2-HMAC-SHA256
- Requires `HOP3_SECRET_KEY` environment variable

```python
# Credential encryption flow
key = derive_key(HOP3_SECRET_KEY, salt)
fernet = Fernet(key)
encrypted = fernet.encrypt(credential.encode())
```

### 5.3 Secure Communication

- SSH tunnel support for CLI-server communication
- HTTPS support with automatic certificate management
- Input validation on all API endpoints
- SQL injection prevention via parameterized queries

### 5.4 Security Audit Outcomes

| Vulnerability | Status | Mitigation |
|--------------|--------|------------|
| JWT Token Tampering | Fixed | Signature verification enforced |
| SQL Injection | Fixed | Parameterized queries throughout |
| Hardcoded Passwords | Fixed | Removed, environment-based config |
| Authentication Bypass | Fixed | Middleware validates all non-public routes |
| Credential Exposure | Fixed | Encrypted storage with Fernet |

### 5.5 Health Check System

Server startup includes automated health checks:

```bash
$ hop3 system:check
✓ hop3-server: running
✓ nginx: running
✓ PostgreSQL: connection successful
✓ Redis: connection successful
✓ Filesystem: permissions OK
✓ Disk space: 45GB available
```

---

## 6. Testing Infrastructure

### 6.1 Test Layers

| Layer | Count | Speed | Scope |
|-------|-------|-------|-------|
| Unit | 361 | < 1s each | Individual functions/classes |
| Integration | 247 | ~10s total | Component interaction |
| System | 13 | ~30s | Full app with CLI in Docker |
| E2E | 17 | 10-20 min | Complete deployment workflows |

**Total: 638 tests**

### 6.2 Test Infrastructure

```
packages/hop3-server/tests/
├── a_unit/          # Fast, isolated tests
├── b_integration/   # Component interaction tests
├── c_system/        # Docker-based system tests
└── d_e2e/           # Full deployment tests

packages/hop3-testing/
├── catalog/         # Test catalog (test.toml support)
├── cli/             # CLI commands (hop3-test)
├── runners/         # Test execution engines
├── results/         # Result storage and reporting
├── selector/        # Test selection logic
└── targets/         # Deployment targets (Docker, SSH)
```

### 6.3 Unified Test Runner

```bash
# System testing (deploys Hop3 first)
hop3-test system --docker

# App testing (uses pre-built image)
hop3-test apps

# Quick developer mode
hop3-test dev

# CI mode with reporting
hop3-test ci --report html
```

### 6.4 CI/CD Integration

- GitHub Actions for automated testing
- SourceHut builds for multiple distributions
- Docker-based reproducible test environments
- Coverage reporting with pytest-cov

---

## 7. Deployment Support

### 7.1 Supported Languages

| Language | Frameworks | Status |
|----------|-----------|--------|
| Python | Flask, Django, FastAPI, Litestar, Starlette, Bottle, Falcon, Eve, DRF, Pyramid, Sanic, Robyn | ✅ Stable |
| JavaScript/Node | Express, Fastify, Next.js, Nuxt.js, Astro, Eleventy, NestJS | ✅ Stable |
| Go | Gin, Fiber, Hugo | ✅ Stable |
| Ruby | Rails, Sinatra, Jekyll | ✅ Stable |
| Rust | Axum, Actix-web | ✅ Stable |
| Java | Spring Boot, Quarkus | ✅ Stable |
| PHP | Laravel, Symfony | ✅ Stable |
| Elixir | Phoenix | ✅ Stable |
| .NET | ASP.NET Core | ✅ Stable |

### 7.2 Backing Services (Addons)

| Service | Features |
|---------|----------|
| PostgreSQL | Managed databases, automatic backups, credential injection |
| MySQL | Database provisioning, SSL support |
| Redis | Cache/session store, connection pooling |

### 7.3 Proxy Support

| Proxy | Features |
|-------|----------|
| Nginx | Default, SSL termination, WebSocket support |
| Caddy | Automatic HTTPS, HTTP/3 |
| Traefik | Dynamic configuration, Let's Encrypt |

### 7.4 Configuration

Applications are configured via `hop3.toml`:

```toml
[metadata]
id = "myapp"
version = "1.0.0"
title = "My Application"

[build]
packages = ["python3", "python3-pip"]

[run]
start = "gunicorn app:app --bind 0.0.0.0:$PORT"

[env]
ENVIRONMENT = "production"

[port]
web = 8000

[healthcheck]
path = "/health"
timeout = 30
```

---

## 8. Documentation

### 8.1 Documentation Site

Built with Zensical static site generator, organized into 7 sections:

| Section | Content |
|---------|---------|
| **Home** | Overview, features |
| **Get Started** | Installation, quickstart, installer options |
| **Guides** | User guide, backup/restore, migration, FAQ |
| **Tutorials** | 33 step-by-step tutorials across 9 languages |
| **Developers** | Architecture, testing, plugins, packages |
| **Blog** | Release announcements, conference talks |
| **Reference** | CLI reference, configuration, policies |

### 8.2 Tutorial System

Tutorials serve dual purposes:
1. **Documentation**: Step-by-step deployment guides
2. **Testing**: Automated validation via custom markdown syntax

~~~markdown
```bash exec id=create-project
mkdir myapp && cd myapp
```

```assert file-exists path=myapp
```
~~~

A conversion pipeline transforms test syntax to standard markdown for documentation rendering.

A generic tool, [Validoc](https://github.com/abilian/validoc), has been developped during the project for this purpose, and published as an independent project.

### 8.3 Cheat Sheets

- **CLI Cheat Sheet**: Quick command reference for daily operations
- **Testing Cheat Sheet**: Developer reference for running tests

---

## 9. Project Status

### 9.1 NGI Deliverables Summary

| Task | Status | Completion |
|------|--------|------------|
| T1: Nix Build Plugins | Not Started | 0% |
| T2: Nix Runtime | Not Started | 0% |
| T3: Security & Resilience | In Progress | 75% (6/8) |
| T4: Packaged Applications | In Progress | 0% (0/20) |
| T5: Dissemination & Engagement | In Progress | 60% (3/5) |

### 9.2 Completed Milestones

**T3: Security & Resilience**
- ✅ M3.1 Backing services (PostgreSQL, MySQL, Redis)
- ✅ M3.2 Upgrades (Alembic migrations)
- ✅ M3.3 Backups (full backup/restore system)
- ✅ M3.4 Testing framework (638 tests)
- ✅ M3.6 CLI (basic)
- ✅ M3.7 Web UI (basic)

**T5: Dissemination & Engagement**
- ✅ M5.2 Documentation (complete site with tutorials)
- ✅ M5.4 Conference presentations (OW2Con, OSXP)

### 9.3 In Progress

- M3.5 Firewalls (WAF integration)
- M3.8 Security audit outcomes
- M5.1 Website enrichment
- T4 Packaged applications (20 apps planned)

---

## 10. Future Work

### 10.1 Nix Integration (T1, T2)

The primary focus for Q1 2026 is Nix integration:

**M1.1 Nix Native Builder:**
- Support applications with existing Nix expressions
- Integrate flake-based builds into Hop3 workflow

**M1.2 Nix Alternatives to Existing Builders:**
- Nix-based Python, Node, Ruby, Go, Rust, Java builders
- Reproducible builds across environments

**M2.1-M2.3 Nix Runtime:**
- Nix-managed runtime environments
- Specification, beta, and production releases

### 10.2 Packaged Applications (T4)

20 popular open-source applications to be packaged:

| Sprint | Applications |
|--------|-------------|
| A | WordPress, NextCloud, Ghost, HedgeDoc, Gitea |
| B | Discourse, Mastodon, Matrix/Synapse, Plausible, Umami |
| C | Mattermost, Rocket.Chat, BookStack, Wiki.js, Etherpad |
| D | Kanboard, Wekan, Invoice Ninja, Monica CRM, Cal.com |

### 10.3 Additional Enhancements

- Web UI completion (app editing, service management)
- Video documentation and screencasts
- Formal security audit
- Production verification across multiple environments

---

## 11. Conclusion

Hop3 has achieved significant milestones toward its goal of providing a simple, secure, and sovereign platform for web application deployment. The client-server architecture provides a solid foundation, the plugin system enables extensibility, and the comprehensive test suite ensures reliability.

Key accomplishments include:
- Production-ready deployment for 9 programming languages
- Secure credential management with encryption at rest
- 638 automated tests ensuring software quality
- Complete documentation with 33 tutorials

The project is well-positioned for the Nix integration work planned for Q1 2026, which will bring reproducible builds and enhanced dependency management to the platform.

---

## Appendix A: Glossary

| Term | Definition |
|------|------------|
| **Addon** | Backing service (database, cache) managed by Hop3 |
| **Builder** | Plugin that orchestrates the build process |
| **Deployer** | Plugin that runs built applications |
| **Toolchain** | Language-specific build logic |
| **Proxy** | Reverse proxy for routing HTTP traffic |

## Appendix B: References

1. Hop3 GitHub Repository: https://github.com/abilian/hop3
2. Hop3 Documentation: https://hop3.cloud
3. NGI0 Commons Fund: https://nlnet.nl/commonsfund/
4. 12-Factor App Methodology: https://12factor.net/
5. Pluggy Documentation: https://pluggy.readthedocs.io/

## Appendix C: Version History

| Version | Date | Changes |
|---------|------|---------|
| 1.0 | February 2026 | Initial draft |

---

*This report was prepared as part of the NGI0 Commons Fund grant for the "Nix Integration for Hop3" project.*
