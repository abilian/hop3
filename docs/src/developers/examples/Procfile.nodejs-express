# Procfile - Node.js/Express Application Example
# Simple convention-based configuration

# Pre-build hooks
prebuild: npm ci && npm run build

# Pre-run hooks
prerun: npm run migrate

# Main web process
web: node dist/server.js

# Alternative: use npm start
# web: npm start

# Background worker (optional)
worker: node dist/worker.js
