[test]
name = "nix-gotosocial-handcrafted"
category = "nix-app"
tier = "medium"
priority = "P1"
description = "GoToSocial deployed via Nix (hand-crafted)"

[test.requirements]
targets = ["docker", "remote"]
services = ["nix"]

[test.metadata]
author = "hop3-team"
covers = ["golang", "nix", "sqlite", "fediverse"]

[deployment]
path = "."
type = "nix"

[[validations]]
type = "http"
path = "/api/v1/instance"
[validations.expect]
status = 200
