[test]
name = "monica"
category = "docker-app"
tier = "very-slow"  # NodeSource install + Laravel Mix + npm run production ~15 min
priority = "P1"

[test.requirements]
services = ["docker", "mysql"]

[[validations]]
type = "http"
path = "/login"

[validations.expect]
status = 200
