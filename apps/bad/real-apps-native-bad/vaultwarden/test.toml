[test]
name = "vaultwarden"
category = "native-app"
tier = "very-slow"
priority = "P1"
description = "Vaultwarden deployed natively (Rust compile)"

[test.requirements]
targets = ["docker", "remote"]

[test.metadata]
author = "hop3-team"
covers = ["rust", "sqlite"]

[deployment]
path = "."

[[validations]]
type = "http"
path = "/alive"
[validations.expect]
status = 200
