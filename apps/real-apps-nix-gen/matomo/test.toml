# Test definition for Matomo built with Nix

[test]
name = "nix-matomo"
category = "nix-app"
tier = "medium"
priority = "P2"  # Known: returns 500 without app-specific config
description = "Matomo web analytics platform built and deployed using Nix"

[test.requirements]
targets = ["docker", "remote"]
services = ["mysql", "nix"]

[test.metadata]
author = "hop3-team"
covers = ["php", "matomo", "nix", "analytics"]

[deployment]
path = "."
type = "nix"

[[validations]]
type = "http"
path = "/"
[validations.expect]
status = 200
contains = "Matomo"
