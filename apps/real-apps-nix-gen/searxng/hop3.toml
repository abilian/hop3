# Hop3 Configuration for SearXNG (Nix build via nixpkgs)
# Privacy-respecting metasearch engine

[metadata]
id = "searxng"
description = "Privacy-respecting, hackable metasearch engine"

[build]
builder = "nix"

[nix]
template = "nixpkgs-wrapper"
nixpkgs-package = "searxng"
exec-target = "searxng-run"
extra-paths = ['${searxng}/bin']

[nix.env-exports]
SEARXNG_SECRET = "${SEARXNG_SECRET:-hop3-searxng-secret-change-me}"
SEARXNG_PORT = "${PORT:-8080}"
SEARXNG_BIND_ADDRESS = "0.0.0.0"

[nix.runtime-env]
SEARXNG_SECRET = "hop3-searxng-secret-change-me"

[env]
SEARXNG_SECRET = "hop3-searxng-secret-change-me"

[run]
start-timeout = 120

[test]
priority = "P2"
tier = "slow"
author = "hop3-team"
targets = ["docker", "remote"]
covers = ["python", "searxng", "nix"]

[[test.validations]]
path = "/healthz"
status = 200
contains = "OK"
