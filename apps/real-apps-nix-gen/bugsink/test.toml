[test]
name = "nix-bugsink"
category = "nix-app"
tier = "slow"
priority = "P1"
description = "Bugsink via Nix (python-venv template)"

[test.requirements]
targets = ["docker", "remote"]
services = ["nix", "postgresql"]

[test.metadata]
author = "hop3-team"
covers = ["python", "django", "nix", "postgres"]

[deployment]
path = "."
type = "nix"

[[validations]]
type = "http"
path = "/accounts/login/"
[validations.expect]
status = 200
