[test]
name = "gatus"
category = "native-app"
tier = "slow"
priority = "P2"
description = "Gatus status pages via Go build from source"

[test.requirements]
targets = ["docker", "remote"]

[test.metadata]
author = "hop3-team"
covers = ["golang", "sqlite", "monitoring"]

[deployment]
path = "."

[[validations]]
type = "http"
path = "/health"
[validations.expect]
status = 200
