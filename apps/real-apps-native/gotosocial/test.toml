[test]
name = "gotosocial"
category = "native-app"
tier = "medium"
priority = "P1"
description = "GoToSocial deployed natively"

[test.requirements]
targets = ["docker", "remote"]

[test.metadata]
author = "hop3-team"
covers = ["golang", "sqlite", "fediverse"]

[deployment]
path = "."

[[validations]]
type = "http"
path = "/api/v1/instance"
[validations.expect]
status = 200
