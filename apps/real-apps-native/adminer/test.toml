[test]
name = "adminer"
category = "native-app"
tier = "fast"
priority = "P1"
description = "Adminer deployed natively"

[test.requirements]
targets = ["docker", "remote"]
services = []

[test.metadata]
author = "hop3-team"
covers = ["php"]

[deployment]
path = "."

[[validations]]
type = "http"
path = "/"
[validations.expect]
status = 200
