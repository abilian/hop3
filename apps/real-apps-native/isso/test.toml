[test]
name = "isso"
category = "native-app"
tier = "medium"
priority = "P2"
description = "Isso commenting system"

[test.requirements]
targets = ["docker", "remote"]
services = []

[test.metadata]
author = "hop3-team"
covers = ["python", "isso"]

[deployment]
path = "."

[[validations]]
type = "http"
path = "/"
[validations.expect]
status = 400
