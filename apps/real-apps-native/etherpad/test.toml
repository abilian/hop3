[test]
name = "etherpad"
category = "native-app"
tier = "medium"
priority = "P1"
description = "Etherpad deployed natively"

[test.requirements]
targets = ["docker", "remote"]
services = []

[test.metadata]
author = "hop3-team"
covers = ["nodejs"]

[deployment]
path = "."

[[validations]]
type = "http"
path = "/"
[validations.expect]
status = 200
