[test]
name = "jenkins"
category = "native-app"
tier = "slow"
priority = "P1"
description = "Jenkins deployed natively"

[test.requirements]
targets = ["docker", "remote"]
services = []

[test.metadata]
author = "hop3-team"
covers = ["java"]

[deployment]
path = "."

[[validations]]
type = "http"
path = "/login"
[validations.expect]
status = 200
