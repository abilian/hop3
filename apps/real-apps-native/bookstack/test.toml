[test]
name = "bookstack"
category = "native-app"
tier = "slow"
priority = "P2"
description = "BookStack wiki - Laravel PHP app with MySQL"

[test.requirements]
targets = ["docker", "remote"]
services = ["mysql"]

[test.metadata]
author = "hop3-team"
covers = ["php", "laravel", "mysql"]

[deployment]
path = "."

[[validations]]
type = "http"
path = "/login"
[validations.expect]
status = 200

