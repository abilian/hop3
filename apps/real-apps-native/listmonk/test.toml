[test]
name = "listmonk"
category = "native-app"
tier = "medium"
priority = "P2"
description = "Listmonk newsletter manager"

[test.requirements]
targets = ["docker", "remote"]
services = ["postgresql"]

[test.metadata]
author = "hop3-team"
covers = ["golang", "listmonk"]

[deployment]
path = "."

[[validations]]
type = "http"
path = "/"
[validations.expect]
status = 200
