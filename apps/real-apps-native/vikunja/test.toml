[test]
name = "vikunja"
category = "native-app"
tier = "medium"
priority = "P1"
description = "Vikunja deployed natively"

[test.requirements]
targets = ["docker", "remote"]
services = ["postgresql"]

[test.metadata]
author = "hop3-team"
covers = ["golang", "postgresql"]

[deployment]
path = "."

[[validations]]
type = "http"
path = "/api/v1/info"
[validations.expect]
status = 200
