[test]
name = "stirling-pdf"
category = "native-app"
tier = "slow"
priority = "P1"
description = "Stirling-PDF deployed natively"

[test.requirements]
targets = ["docker", "remote"]

[test.metadata]
author = "hop3-team"
covers = ["java", "spring-boot"]

[deployment]
path = "."

[[validations]]
type = "http"
path = "/login"
[validations.expect]
status = 200
