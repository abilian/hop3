[test]
name = "bugsink"
category = "native-app"
tier = "medium"
priority = "P1"
description = "Bugsink error tracking deployed natively"

[test.requirements]
targets = ["docker", "remote"]
services = ["postgresql"]

[test.metadata]
author = "hop3-team"
covers = ["python", "django", "postgres"]

[deployment]
path = "."

[[validations]]
type = "http"
path = "/accounts/login/"
[validations.expect]
status = 200
