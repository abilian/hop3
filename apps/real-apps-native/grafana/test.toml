[test]
name = "grafana"
category = "native-app"
tier = "medium"
priority = "P1"
description = "Grafana deployed natively"

[test.requirements]
targets = ["docker", "remote"]
services = []

[test.metadata]
author = "hop3-team"
covers = ["golang"]

[deployment]
path = "."

[[validations]]
type = "http"
path = "/api/health"
[validations.expect]
status = 200
