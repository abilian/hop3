[test]
name = "radicale"
category = "native-app"
tier = "fast"
priority = "P1"
description = "Radicale deployed natively"

[test.requirements]
targets = ["docker", "remote"]
services = []

[test.metadata]
author = "hop3-team"
covers = ["python"]

[deployment]
path = "."

[[validations]]
type = "http"
path = "/.web/"
[validations.expect]
status = 200
