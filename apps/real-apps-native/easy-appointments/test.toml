[test]
name = "easy-appointments"
category = "native-app"
tier = "medium"
priority = "P1"
description = "Easy Appointments deployed natively"

[test.requirements]
targets = ["docker", "remote"]
services = ["mysql"]

[test.metadata]
author = "hop3-team"
covers = ["php", "mysql"]

[deployment]
path = "."

[[validations]]
type = "http"
path = "/"
[validations.expect]
status = 307
