[test]
name = "dolibarr"
category = "native-app"
tier = "medium"
priority = "P1"
description = "Dolibarr deployed natively"

[test.requirements]
targets = ["docker", "remote"]
services = ["postgresql"]

[test.metadata]
author = "hop3-team"
covers = ["php", "postgresql"]

[deployment]
path = "."

[[validations]]
type = "http"
path = "/"
[validations.expect]
status = 200
