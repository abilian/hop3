[test]
name = "writefreely"
category = "native-app"
tier = "medium"
priority = "P1"
description = "WriteFreely federated blog deployed natively"

[test.requirements]
targets = ["docker", "remote"]

[test.metadata]
author = "hop3-team"
covers = ["golang", "sqlite", "fediverse"]

[deployment]
path = "."

[[validations]]
type = "http"
path = "/"
[validations.expect]
status = 200
