[test]
name = "hedgedoc"
category = "native-app"
tier = "slow"
priority = "P1"
description = "Hedgedoc deployed natively"

[test.requirements]
targets = ["docker", "remote"]
services = ["postgresql"]

[test.metadata]
author = "hop3-team"
covers = ["nodejs", "postgresql"]

[deployment]
path = "."

[[validations]]
type = "http"
path = "/"
[validations.expect]
status = 200
