[test]
name = "xwiki"
category = "native-app"
tier = "very-slow"
priority = "P2"
description = "XWiki enterprise wiki (slow Java startup)"

[test.requirements]
targets = ["docker", "remote"]
services = ["postgresql"]

[test.metadata]
author = "hop3-team"
covers = ["java", "xwiki"]

[deployment]
path = "."

[[validations]]
type = "http"
path = "/xwiki/"
[validations.expect]
status = 302
