[test]
name = "invoice-ninja"
category = "native-app"
tier = "slow"
priority = "P1"
description = "Invoice Ninja deployed natively"

[test.requirements]
targets = ["docker", "remote"]
services = ["mysql"]

[test.metadata]
author = "hop3-team"
covers = ["php", "laravel", "mysql"]

[deployment]
path = "."

[[validations]]
type = "http"
path = "/"
[validations.expect]
status = 302
