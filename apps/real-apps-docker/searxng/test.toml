[test]
name = "searxng"
category = "docker-app"
tier = "medium"
priority = "P0"

[test.requirements]
services = ["docker"]

[[validations]]
type = "http"
path = "/healthz"

[validations.expect]
status = 200
