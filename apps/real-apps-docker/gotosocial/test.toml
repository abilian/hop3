[test]
name = "gotosocial"
category = "docker-app"
tier = "medium"
priority = "P1"

[test.requirements]
services = ["docker"]

[[validations]]
type = "http"
path = "/api/v1/instance"
[validations.expect]
status = 200
