[test]
name = "matrix-synapse"
category = "docker-app"
tier = "medium"
priority = "P1"

[test.requirements]
services = ["docker", "postgresql"]

[[validations]]
type = "http"
path = "/_matrix/client/versions"

[validations.expect]
status = 200
