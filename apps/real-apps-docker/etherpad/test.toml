[test]
name = "etherpad"
category = "docker-app"
tier = "medium"
priority = "P0"

[test.requirements]
services = ["docker"]

[[validations]]
type = "http"
path = "/health"

[validations.expect]
status = 200
