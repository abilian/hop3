[test]
name = "miniflux"
category = "docker-app"
tier = "medium"
priority = "P1"

[test.requirements]
services = ["docker", "postgresql"]

[[validations]]
type = "http"
path = "/healthcheck"

[validations.expect]
status = 200
