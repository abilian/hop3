[test]
name = "cryptpad"
category = "docker-app"
tier = "medium"
priority = "P1"

[test.requirements]
services = ["docker"]

[[validations]]
type = "http"
path = "/api/config"

[validations.expect]
status = 200
