[test]
name = "mediawiki"
category = "docker-app"
tier = "medium"
priority = "P2"

[test.requirements]
services = ["docker", "postgresql"]

[[validations]]
type = "http"
path = "/api.php"
[validations.expect]
status = 200
