[test]
name = "directus"
category = "docker-app"
tier = "slow"
priority = "P1"

[test.requirements]
services = ["docker", "postgresql"]

[[validations]]
type = "http"
path = "/admin/login"
[validations.expect]
status = 200
