[test]
name = "stirling-pdf"
category = "docker-app"
tier = "very-slow"
priority = "P1"

[test.requirements]
services = ["docker"]

[[validations]]
type = "http"
path = "/login"
[validations.expect]
status = 200
