[test]
name = "umami"
category = "docker-app"
tier = "medium"
priority = "P1"

[test.requirements]
services = ["docker", "postgresql"]

[[validations]]
type = "http"
path = "/api/heartbeat"

[validations.expect]
status = 200
