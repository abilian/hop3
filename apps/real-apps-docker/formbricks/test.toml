[test]
name = "formbricks"
category = "docker-app"
tier = "slow"
priority = "P1"

[test.requirements]
services = ["docker", "postgresql"]

[[validations]]
type = "http"
path = "/health"

[validations.expect]
status = 200
