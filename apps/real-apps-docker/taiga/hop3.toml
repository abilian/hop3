# Hop3 Configuration for Taiga (Docker-based)
# Open source agile project management

[metadata]
id = "taiga"
version = "6.8"
title = "Taiga"
description = "Open source project management for agile teams (Scrum/Kanban)"
homepage = "https://taiga.io/"
license = "MPL-2.0"
categories = ["project-management", "agile", "scrum", "kanban"]

[[addons]]
type = "postgres"

[env]
TAIGA_SECRET_KEY = "change-me-to-random-string"
ENABLE_TELEMETRY = "False"

[run]
# Taiga's container entrypoint runs ALL Django migrations + collectstatic
# (~1000 files) + loaddata before gunicorn binds, so first boot needs well over
# the 60s default before /api/v1/ answers. Migrate-on-boot, not a crash.
start-timeout = 300

[healthcheck]
path = "/api/v1/"

[test]
priority = "P2"
tier = "very-slow"

[[test.validations]]
path = "/api/v1/"
status = 200
contains = "api/v1"
