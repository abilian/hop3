[test]
name = "monica"
category = "docker-app"
tier = "slow"
priority = "P1"

[test.requirements]
services = ["docker", "mysql"]

[[validations]]
type = "http"
path = "/login"

[validations.expect]
status = 200
