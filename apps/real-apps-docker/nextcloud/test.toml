[test]
name = "nextcloud"
category = "docker-app"
tier = "medium"
priority = "P1"

[test.requirements]
services = ["docker", "postgresql", "redis"]

[[validations]]
type = "http"
path = "/status.php"

[validations.expect]
status = 200
