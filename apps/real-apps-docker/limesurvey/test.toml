[test]
name = "limesurvey"
category = "docker-app"
tier = "medium"
priority = "P1"

[test.requirements]
services = ["docker", "postgresql"]

[[validations]]
type = "http"
path = "/"

[validations.expect]
status = 200
