[test]
name = "owncast"
category = "docker-app"
tier = "medium"
priority = "P1"

[test.requirements]
services = ["docker"]

[[validations]]
type = "http"
path = "/api/status"
[validations.expect]
status = 200
