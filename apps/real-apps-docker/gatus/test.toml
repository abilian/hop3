[test]
name = "gatus"
category = "docker-app"
tier = "slow"
priority = "P2"

[test.requirements]
services = ["docker"]

[[validations]]
type = "http"
path = "/health"
[validations.expect]
status = 200
