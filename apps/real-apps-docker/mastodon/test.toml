[test]
name = "mastodon"
category = "docker-app"
tier = "slow"
priority = "P1"

[test.requirements]
services = ["docker", "postgresql", "redis"]

[[validations]]
type = "http"
path = "/health"

[validations.expect]
status = 200
