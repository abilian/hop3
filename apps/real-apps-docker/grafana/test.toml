[test]
name = "grafana"
category = "docker-app"
tier = "medium"
priority = "P0"

[test.requirements]
services = ["docker", "postgresql"]

[[validations]]
type = "http"
path = "/api/health"

[validations.expect]
status = 200
