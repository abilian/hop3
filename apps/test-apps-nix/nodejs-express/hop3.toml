# hop3.toml - Hop3 configuration for Nix-based Node.js app

[build]
builder = "nix"

[test]
priority = "P1"
tier = "medium"
author = "hop3-team"
targets = ["docker", "remote"]
covers = ["nodejs", "express", "nix"]

[[test.validations]]
path = "/"
status = 200
contains = "Hello World"
