# hop3.toml - Hop3 configuration for Nix-based Clojure app

[build]
builder = "nix"

[test]
priority = "P2"
tier = "slow"
author = "hop3-team"
targets = ["docker", "remote"]
covers = ["clojure", "nix"]

[[test.validations]]
path = "/"
status = 200
contains = "Hello World"
