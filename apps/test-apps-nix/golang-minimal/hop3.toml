# hop3.toml - Hop3 configuration for Nix-based minimal Go app

[build]
builder = "nix"

[test]
priority = "P1"
tier = "fast"
author = "hop3-team"
targets = ["docker", "remote"]
covers = ["golang", "nix"]

[[test.validations]]
path = "/"
status = 200
contains = "Hello World"
